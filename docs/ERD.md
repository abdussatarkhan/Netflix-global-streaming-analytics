# Database Architecture & Entity-Relationship Diagram (ERD)

## 1. Entity-Relationship Diagram (Mermaid)

```mermaid
erDiagram
    TITLES ||--o| TITLE_EMBEDDINGS : "has embedding (1:1)"
    TITLES ||--o{ USER_INTERACTIONS : "receives interaction (1:N)"

    TITLES {
        VARCHAR_32 show_id PK "Unique content identifier"
        VARCHAR_32 type "Movie or TV Show"
        VARCHAR_512 title "Content title"
        TEXT director "Director names"
        TEXT cast_members "Starring actors"
        VARCHAR_256 country "Production country"
        DATE date_added "Catalog addition date"
        INTEGER release_year "Original release year [1900-2100]"
        VARCHAR_32 rating "Age rating (e.g. TV-MA, PG-13)"
        VARCHAR_64 duration "Runtime (min or seasons)"
        TEXT listed_in "Comma-separated genres"
        TEXT description "Full plot synopsis"
        TIMESTAMP created_at "Record creation timestamp"
        TIMESTAMP updated_at "Last update timestamp"
    }

    TITLE_EMBEDDINGS {
        VARCHAR_32 show_id PK, FK "References titles.show_id (CASCADE)"
        VECTOR_384 embedding "Dense vector generated by all-MiniLM-L6-v2"
        VARCHAR_64 model_version "Embedding model identifier"
        TIMESTAMP updated_at "Embedding calculation timestamp"
    }

    USER_INTERACTIONS {
        BIGSERIAL interaction_id PK "Auto-incrementing telemetry ID"
        VARCHAR_64 user_id "User identifier"
        VARCHAR_32 show_id FK "References titles.show_id (CASCADE)"
        VARCHAR_32 interaction_type "watch | like | save | skip"
        NUMERIC_5_2 watch_duration_pct "Completion percentage [0.00 - 100.00]"
        TIMESTAMP timestamp "Event occurrence timestamp"
    }
```

---

## 2. Table Schemas & Indexing Strategy

### `titles` Table
* **Primary Key:** `show_id` (VARCHAR 32)
* **Indexes:**
  - `idx_titles_release_year` on `release_year` (B-Tree)
  - `idx_titles_type` on `type` (B-Tree)
  - `idx_titles_country` on `country` (B-Tree)
  - `idx_titles_rating` on `rating` (B-Tree)

### `title_embeddings` Table
* **Primary Key:** `show_id` (VARCHAR 32, FK to `titles.show_id` ON DELETE CASCADE)
* **Vector Index:**
  - `idx_title_embeddings_hnsw` on `embedding` USING `hnsw (embedding vector_cosine_ops) WITH (m = 16, ef_construction = 64)`
  - *Rationale:* HNSW provides sub-linear $O(\log N)$ search latency with high recall ($>98\%$) compared to IVFFlat, eliminating periodic index rebuild requirements.

### `user_interactions` Table
* **Primary Key:** `interaction_id` (BIGSERIAL)
* **Foreign Key:** `show_id` referencing `titles.show_id` (ON DELETE CASCADE)
* **Composite Indexes:**
  - `idx_user_interactions_user_time` on `(user_id, timestamp DESC)` for rapid extraction of a user's recent watch history in chronological order.
  - `idx_user_interactions_show` on `show_id` for item-level engagement aggregation.
  - `idx_user_interactions_type` on `interaction_type` for filtering explicit feedback (`like`, `save`).
